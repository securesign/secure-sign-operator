---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: tasoperatorconfigs.rhtas.redhat.com
spec:
  group: rhtas.redhat.com
  names:
    kind: TASOperatorConfig
    listKind: TASOperatorConfigList
    plural: tasoperatorconfigs
    singular: tasoperatorconfig
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - description: The detected platform type
      jsonPath: .spec.platform
      name: Platform
      type: string
    - description: How the platform was detected
      jsonPath: .status.detectionMethod
      name: Detection Method
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: TASOperatorConfig is the Schema for the operatorconfigs API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TASOperatorConfigSpec defines the desired state of TASOperatorConfig
            properties:
              platform:
                description: Platform type detected or configured (e.g., "openshift",
                  "kubernetes")
                enum:
                - openshift
                - kubernetes
                type: string
            type: object
          status:
            description: TASOperatorConfigStatus defines the observed state of TASOperatorConfig
            properties:
              detectionMethod:
                description: DetectionMethod indicates how the platform was determined
                enum:
                - auto-detected
                - command-line
                type: string
              detectionTimestamp:
                description: DetectionTimestamp records when the platform was detected
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}

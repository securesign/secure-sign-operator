apiVersion: rhtas.redhat.com/v1alpha1
kind: Fulcio
metadata:
  labels:
    app.kubernetes.io/name: securesign-sample
    app.kubernetes.io/instance: securesign-sample
    app.kubernetes.io/part-of: trusted-artifact-signer
  name: fulcio-sample
spec:
  externalAccess:
    enabled: true
  config:
    OIDCIssuers:
      - ClientID: "trusted-artifact-signer"
        IssuerURL: "https://your-oidc-issuer-url"
        Issuer: "https://your-oidc-issuer-url"
        Type: "email"
  certificate:
    caType: file
    organizationName: Red Hat
    organizationEmail: jdoe@redhat.com
    commonName: fulcio.hostname
    # To use PKCS#11 instead of file-based CA, replace the above with:
    # caType: pkcs11
    # pkcs11:
    #   pin: "secretpin123"
    #   tokenLabel: "fulcio"
    #   libraryPath: "/usr/lib64/pkcs11/libsofthsm2.so"
    #   initContainer:
    #     image: "quay.io/rh-ee-sacm/softhsm-init:latest"
    #     env:
    #       - name: SOFTHSM2_CONF
    #         value: /etc/softhsm/softhsm2.conf
    #     volumes:
    #       - name: softhsm-config
    #         mountPath: /etc/softhsm
    #         inlineData:
    #           softhsm2.conf: |
    #             directories.tokendir = /var/lib/hsm/tokens
    #             objectstore.backend = file
    #   serverEnv:
    #     - name: SOFTHSM2_CONF
    #       value: /etc/softhsm/softhsm2.conf
